{
  "name": "rag-pipeline-lambdas",
  "version": "1.0.0",
  "type": "module",
  "description": "Lambda handlers for RAG pipeline — query and ingestion",
  "scripts": {
    "build": "npm ci --omit=dev"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent": "^3.650.0",
    "@aws-sdk/client-bedrock-agent-runtime": "^3.650.0"
  }
}
